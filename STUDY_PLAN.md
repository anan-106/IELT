# 自动学习计划

## 默认目标

默认 **30 天完成全部当前可学习卡**（538 主词训练 + 488 总表词义训练 + Academic）。

每天打开页面时，系统自动计算：

`今日新学 = ceil(剩余未学卡 / 剩余计划天数)`

如果前一天少学，后续每天的新词量会自动增加；如果提前完成，新词量会自动减少。

## 复习量

复习不设置固定配额。当天所有已经到期的卡优先复习，数量由实际学习历史和跨天间隔自动决定。

当前跨天间隔：`1 → 2 → 6 → 31 → 60 → 120 天`。

当天答错的卡不算新的跨天复习节点，而是在当天后续题目中分开重新证明；需要重新答对 2 次，中途再错则重新从 2 次开始。

## 今日任务

页面自动显示：

- 计划总卡数
- 已完成新学
- 剩余卡数
- 今日需要新学多少
- 今日到期复习多少
- 今日基础任务总量
- 未来 7 天预计新学 / 复习数量
- 当前计划日（Day X / 30）和目标日期

未来 7 天复习量是估算值，实际数量会随答错、当天强化和实际完成进度变化。

## 修改目标

在“设置 → 自动学习计划”中可以：

- 修改目标天数（7–180 天）
- 开启 / 关闭自动控制每日新词
- 从今天重新开始计划

计划配置和学习记录一起保存在 `ielt-memory-v3` 本地数据中，也会随 JSON 备份一起导出。

## 本地长期保存

不建议直接双击 `index.html` 依赖 `file://` 的 localStorage 行为。

Windows 下推荐双击：

`启动IELT.bat`

它会在本机启动：

`http://127.0.0.1:8787/`

只要一直使用同一个浏览器和这个本地地址，学习记录、错词状态、30 天计划和跨天复习阶段都会持续保存在浏览器本地。
