# 张巍 GRE 数学满分宝典：本地原书练习模块

## 使用

打开 `gre-prep` 版本的网站 → **Quant** → **导入这份 PDF**，选择本次提供的《张巍GRE数学满分宝典.pdf》。首次显示需要加载 PDF.js；词汇练习不依赖 PDF。PDF 不提交到仓库、不上传到服务器：浏览器先校验内容，再存入 IndexedDB。正常完成后同一浏览器下次访问会自动恢复。请通过 HTTPS 或 localhost 访问，不要直接双击 HTML。

文件指纹：357 页，110,215,852 字节；SHA-256：
`b8df6e4b3f051987557a5db14a008b367b95181a0e3229e71d6fa519b33fb20f`。

PDF 页码 = 原书正文页码 + 4。不同版本或重新压缩的 PDF 会被拒绝，避免错配题目和答案。

在 **数学词汇** 中进行英文→中文、中文→英文或混合选择题。在原有 **错题本** 页面下方查看独立的数学题 / 数学词汇错题；在 **今日计划** 下方查看到期任务和未来七天的已知复习量。在 **设置 → 数学专项设置与备份** 调整每天新学数量、导出或合并专用备份。原站的普通备份不包含本模块；数学备份不包含 PDF 文件。

## 覆盖清单

|来源部分|收录|
|---|---:|
|第一章：算术、代数、几何、资料分析的综合训练|365 题|
|第二章：5 套原书综合能力测试，每套 2 节 × 20 题|200 题|
|4.6 数据分析示例|4 个带原解析的阅读示例，不混入盲测|
|附录：GRE 数学词汇汇总，PDF 345–357 页|386 条原始词条|

目录保留 28 个原书知识小节和 5 套模考。题目、答案和解析索引逐项保留原书题号、PDF 页码；跨页题干 / 解析与图表共用数据有独立索引。图形和数学公式不经过 OCR 或文字重排：章节题显示原题左栏，提交后显示右栏解析；模考题与解析分别取自原书第二、三章。

本模块录入的是原书答案，不声称对全部原题进行了独立数学复核。原书模考保留旧版每节 20 题的结构，不宣称与现行考试题量或时长相同。通用字母输入为 A–H，请依照原题选择实际存在的选项；界面不会通过单选 / 多选控件泄露正确选项数量。

### 原文疑义处理

565 题中 **564 题可自动判分**。`math:mock3:1:13`（第 3 套 Section 1，第 13 题，PDF 274 页；解析 PDF 319 页）：题干使用 `1–4 weeks`，解析使用 `11–14 weeks`。保留原文及原答案 D，但不自动判分、不纳入自动复习成绩。

386 个词条中 **378 条进入自动选择题**。8 条保留原表述并标记待核对：`inverse`、`standard deviation`、`bar graphs/histogram`、`oblique`、`segment of a circle`、`xy-planes`、`be direct(inverse) proportion at/to`、`purchases`。不以未核对释义作为唯一正确选项。重复词条保留其原页位置；相同英文、相同或包含关系的中文释义及已标注重叠组会被排除为干扰项。解释显示原书词义及每个选项的对应释义，没有把生成式解释冒充原解析。

## 记录和复习规则（本网站自定义，不是书中公式）

每题 / 每词分别保存累计作答、正确 / 错误次数、首次 / 最近作答、用时、最近误答、错误原因、笔记、最近 50 次明细和复习状态。更早明细不继续增长，累计次数保留。

正确后按 **1、3、7、14、30 天**推进；日间隔按照本地日历计算。答错重置进度并在 **10 分钟后**到期，同时在本轮最多两次隔题重现（有足够题目时隔三题）。提前练习或同日反复答对不升级长期间隔。错题在后续三个不同到期日答对才变为已巩固；历史不会被删除。

今日计划先放到期 / 逾期，再补当天新学额度；已在当天初次作答的项目会扣减新学额度。七日预测只统计已有记录的到期时间，不推测尚未作答的未来新题，不包含逾期欠账。

## 集成与安全

- 新文件：`gre-math-bank.js`（来源索引）、`gre-math-core.js`（纯判分 / 计划逻辑）、`gre-math-book.js`（界面 / 本地 PDF）、`gre-math-book.css`（限定作用域样式）。
- `index.html` 只增加一个样式和三个脚本标签；不替换原模块。
- 新状态键 `gre-math-book-v1`，PDF 数据库 `gre-math-source-v1`；不修改原站 `gre-prep-v1`。
- 原始 PDF 未发布。PDF.js 固定为 `6.3.289`，先尝试可选的同源 `vendor/pdfjs/`，再尝试 jsDelivr / unpkg。加载失败时提供浏览器本地原书入口，不伪造已经显示成功。
- 如需完全不依赖 CDN，可把该版本官方发行包的 `build/`、`cmaps/`、`standard_fonts/`、`wasm/` 保持结构放到 `vendor/pdfjs/`，并保留发行包的许可证。
- 备份采用独立格式 `gre-math-book-backup`，有版本、字段、ID 和大小校验。按每题最近更新时间合并，不合并两个设备各自增量的明细。笔记和导入字符串以文本渲染，不作为 HTML 执行。
- 浏览器存储不是跨设备同步。清理网站数据 / 隐私窗口关闭 / 配额回收可能造成记录或 PDF 丢失，请定期导出数学备份。保存失败时显示警告，不宣称已经持久化。

## 验证

`node --test tests/math.test.cjs`：11 项测试通过，覆盖完整数量、各套模考、页码 / 图表裁剪元数据、分数 / 百分比 / 多选判分、全部可测词条双向四选项生成、SRS、历史保留和备份输入校验。

`python tests/math-dom.py`：Chromium 中完成离线 DOM 组件测试，包括数学表单、词汇误答重现、笔记转义、状态重新初始化、旧状态键不变、390px 宽度。测试明确使用内存 localStorage 和旧导航夹具；数学 PDF 显示门槛仅在测试中绕过，以验证表单事件。

当前执行环境阻止浏览器访问 localhost，也无法获取真实 PDF.js CDN 文件，因此 **没有完成真实 PDF.js + IndexedDB 的端到端验证，也没有验证生产网站部署**。`tests/browser_smoke.py` 提供在允许 localhost 的环境中执行的额外组件检查，使用标注清楚的 PDF.js 测试替身；真实 PDF 解析显示仍需实际浏览器验收。

## 技术资料

PDF.js 官方示例：https://mozilla.github.io/pdf.js/examples/  
PDF.js 官方发行说明：https://mozilla.github.io/pdf.js/getting_started/  
IndexedDB 官方参考：https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API

以上链接用于实现说明；题目、答案、词汇和章节依据用户提供的 PDF，不由外部题库替换。
