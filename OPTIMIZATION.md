# 优化说明

本轮优化聚焦“学习质量 + 稳定性”，不改变既有 538 / 488 / Academic 数据口径。

## 1. 未完成错词强化恢复

- 任何答错词仍需在后续分开再答对 2 次；
- 未完成状态独立写入 `localStorage`；
- 刷新、关闭页面再打开后仍保留；
- 有未完成强化时，新的“今日学习”会先进入恢复流程；
- 恢复完成后，跨天阶段从明天重新开始。

实现：`session-recovery.js`

## 2. 混淆矩阵与自适应干扰项

系统记录用户真实误选，例如：

- `principal` 曾误选 `principle`；
- 某一干扰项被误选多次。

后续同一张 538 卡再次出现时，会优先把历史误选表达放回错误选项，提高个人针对性。正确答案保持不变。

实现：`adaptive-learning.js`

## 3. 反应时间

- 记录题目出现到作答的时间；
- 正确仍然只按正确处理，不让用户自评“难/简单”；
- 很慢的正确只记录为辅助信号；
- “统计”页显示近期中位反应时间和常见混淆。

## 4. 30 天计划负荷平滑

自动计划仍以：

`剩余未学卡 ÷ 剩余计划天数`

为基准，但新增复习负荷平滑：

- 到期复习较重的日期，适当减少新词；
- 到期复习较轻的日期，适当增加新词；
- 调整幅度保守限制在基准的大约 60%–140%；
- 接近截止日时优先保证完成计划。

实现：`study-plan-preload.js`、`study-plan-ui.js`

## 5. 启动自检与本地数据保护

启动时检查：

- 376 主词是否完整；
- 三类数量是否为 20 / 100 / 256；
- `cardId` 是否重复；
- 词义卡是否缺中文；
- 488 词义 deck 是否为空；
- 538 是否存在缺可测替换的数据；
- `localStorage` 是否可读写。

页面顶部显示“数据自检”状态。设置页可以请求浏览器持久存储，降低浏览器因存储压力自动清理本地学习数据的风险。

实现：`health-check.js`
